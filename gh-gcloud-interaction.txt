# Authenticate the login to the account.
gh auth login

# Clone a GitHub Repo call NodeTutorial under Account AlienShuffle, logged in above.
gh repo clone AlienShuffle/NodeTutorial

# use git commands like commit, push, etc. to maintain the repo with GH.


# Using gcloud command submit a Google Cloud Function from this repository into the Google Cloud account.
# used to configure login to the GCF account, this also sets the project
gcloud init

# Deploy a Cloud Function called helloWorld into the curent project
# don't know why but only worked when I named the .js file function.js
gcloud functions deploy 'testfunc' --runtime nodejs12 --trigger-http --entry-point=helloWorld